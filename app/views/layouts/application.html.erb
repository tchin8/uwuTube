<!DOCTYPE html>
<html>
  <head>
    <title>uwuTube</title>
    <meta property="og:title" content="uwuTube" />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="https://uwuTube.herokuapp.com/" />
    <meta property="og:image" content="<%= image_url('uwutube_logo.png') %>" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>

    <script>
      window.default_thumb="<%= image_url("default_thumb.png") %>"
    </script>

    <% if current_user %>
      <script type="text/javascript" >
        window.currentUser = {
          "id": <%= current_user.id %>,
          "email": "<%= current_user.email %>",
          "fname": "<%= current_user.fname %>",
          "lname": "<%= current_user.lname %>",
        };
      </script>
    <% end %>

  </head>

  <body>
    <%= yield %>
  </body>
</html>
